/**
 * Exchange adapter schemas and types
 *
 * Provides type-safe contracts for exchange adapters:
 * - UnifiedCandle: Common candle format for all exchanges
 * - ExchangeAdapterEvents: Typed event map for adapter events
 * - IExchangeAdapter: Interface that all exchange adapters must implement
 */
export * from './exchange-adapter.schema';
export * from './rest-client.schema';
